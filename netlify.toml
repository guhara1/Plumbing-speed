# Netlify 배포 설정
# 사이트 소스는 data/*.js + build.js 이고, 실제 서빙 대상은 dist/ 입니다.
[build]
  command = "node build.js"
  publish = "dist"

# 없는 경로는 생성된 404 페이지로
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
